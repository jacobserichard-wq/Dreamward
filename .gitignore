# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# session-local notes (audits, step reports, scratch — generated by Claude Code, not part of the repo)
session-notes/

# Pager debris from stuck `less` session, May 9-10
# These files have names too long for Windows tooling to delete cleanly.
# Safe to ignore � not imported, not tracked, not part of any module path.
*green bg*
*there is a modal*
*ingle commit containing*
*lates the table-styled*
*the one piece*
*tyled like a button*
*vReviewModal*
*activeTab*
*item.color*
*ponsive sweep*
*directive on line*
*Risk 5*
*map cleanly*
*moved out of*
*Also consider*
.claude/

*600*
